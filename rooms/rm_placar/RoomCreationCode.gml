if (!instance_exists(obj_control)) instance_create_layer(0, 0, "Instances", obj_control); 
if (!instance_exists(obj_placar)) instance_create_layer(0, 0, "Instances", obj_placar); 

obj_control.level_game = false;

